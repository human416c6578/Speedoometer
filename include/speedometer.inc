#pragma reqlib "speedometer"

native set_user_colors(id, r, g, b);

native toggle_speed(id);
native toggle_fps(id);
native toggle_keys(id);

native get_bool_speed(id);
native get_bool_fps(id);
native get_bool_keys(id);